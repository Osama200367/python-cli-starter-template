"""The `app` package: a beginner-friendly Python CLI starter template.

Keeping the version in one place (here) means any other module can import it
with `from app import __version__` instead of duplicating the string.
"""

__version__ = "0.1.0"
